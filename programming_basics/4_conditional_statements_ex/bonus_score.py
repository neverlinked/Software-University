starting_points = int(input())
if starting_points <= 100:
    bonus_points = 5
elif starting_points <= 1000:
    bonus_points = starting_points * 0.20
elif starting_points > 1000:
    bonus_points = starting_points * 0.10

summed_points = starting_points + bonus_points
second_bonus = 0

if bonus_points % 2 = 0:
    summed_points + 1
elif bonus_points % 10 = 5:
   summed_points +2
   